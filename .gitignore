.idea
/logs
/test/logs
dist
user_agent.csv
__pycache__/
.venv/
# Downloaded 10-K filings and the SEC companyfacts cache: both are re-fetchable from
# data.sec.gov and grow without bound (155 MB / 58 MB after 10 companies). Anchored at the
# root so the small, deliberately committed fixtures under /test/resources/filings stay in.
/filings/
/.companyfacts_cache/

# Generated backtest reports (backtest_report.py). Re-derivable from the DB plus a
# yfinance run, and the CSV is an output to read, not source to review.
/reports/
